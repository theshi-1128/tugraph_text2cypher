我希望你像一个Tugraph数据库前端一样工作，你只需要返回给我cypher语句。下面是一条描述任务的指令，写一条正确的response来完成这个请求。##Instruction: yago包含节点Person、City、Film和边HAS_CHILD、MARRIED、BORN_IN、KNOWS、DIRECTED、WROTE_MUSIC_FOR、ACTED_IN。节点Person有属性name（STRING）、age（INT16）、birthyear（INT16）、gender（INT8）、phone（INT16）。节点City有属性name（STRING）、area（DOUBLE）、population（DOUBLE）。节点Film有属性title（STRING）。边BORN_IN有属性weight（FLOAT）。边KNOWS有属性weight（FLOAT）。边ACTED_IN有属性charactername（STRING）。